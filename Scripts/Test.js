import {Block} from "./Block"
import { BlockContainer } from "./BlockContainer"

function test(){
    console.log("This is a test for Blocks and the BlockContainer");

    var tBlock = new Block("Test Text", " ");
    console.log(tBlock);
}